<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>10.0</LangVersion>
    <RootNamespace>RefugeCooldownOverlay.Core</RootNamespace>
    <!-- Read-only diagnostic library: must stay portable and free of WinForms/Win32 build deps. -->
  </PropertyGroup>

</Project>
